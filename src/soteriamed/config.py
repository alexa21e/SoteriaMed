"""Typed config loading.

Every experiment is a YAML file in ``configs/`` (invariant 3) — no hardcoded
model names in experiment code. Implemented in phase 2, when the first config
exists to load. Intentionally empty until then.
"""
